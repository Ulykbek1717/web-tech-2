# 🚀 Инструкция по деплою проекта на Render

## 📋 Содержание
1. [Подготовка проекта](#1-подготовка-проекта)
2. [Создание аккаунта на Render](#2-создание-аккаунта-на-render)
3. [Деплой бэкенда и фронтенда](#3-деплой-бэкенда-и-фронтенда)
4. [Настройка переменных окружения](#4-настройка-переменных-окружения)
5. [Настройка MongoDB Atlas](#5-настройка-mongodb-atlas)
6. [Проверка деплоя](#6-проверка-деплоя)
7. [Решение проблем](#7-решение-проблем)

---

## 1. Подготовка проекта

### 1.1 Создайте файл `.env.example`

В корне проекта создайте файл `.env.example` со следующим содержимым:

```env
MONGODB_URI=your_mongodb_connection_string_here
JWT_SECRET=your_super_secret_jwt_key_here
PORT=3000
NODE_ENV=production

# Email settings (optional)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password
```

**Не добавляйте файл `.env` в Git!** Убедитесь, что он в `.gitignore`.

### 1.2 Проверьте package.json

Убедитесь, что в `package.json` есть:

```json
{
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
```

### 1.3 Загрузите код на GitHub

```bash
# Перейдите в папку проекта
cd "assignment 3 web 2"

# Инициализируйте Git (если еще не сделали)
git init

# Создайте .gitignore
echo "node_modules/" > .gitignore
echo ".env" >> .gitignore
echo "*.log" >> .gitignore

# Добавьте файлы
git add .
git commit -m "Prepare for deployment"

# Создайте репозиторий на GitHub и подключите
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git branch -M main
git push -u origin main
```

---

## 2. Создание аккаунта на Render

### 2.1 Регистрация

1. Перейдите на [Render.com](https://render.com)
2. Нажмите **"Get Started"** или **"Sign Up"**
3. Зарегистрируйтесь через GitHub (рекомендуется) или email
4. Подтвердите email, если требуется

### 2.2 Подключите GitHub

1. После входа в аккаунт Render, перейдите в **Settings**
2. В разделе **Connected Accounts** подключите GitHub
3. Разрешите Render доступ к вашим репозиториям

---

## 3. Деплой бэкенда и фронтенда

> **Примечание:** Так как ваш фронтенд интегрирован с бэкендом (статические файлы в папке `public`), вам нужно задеплоить только один сервис!

### 3.1 Создайте новый Web Service

1. На дашборде Render нажмите **"New +"** → **"Web Service"**
2. Выберите **"Build and deploy from a Git repository"**
3. Нажмите **"Connect"** напротив вашего GitHub репозитория
4. Если не видите репозиторий, нажмите **"Configure account"** и предоставьте доступ

### 3.2 Настройте параметры деплоя

Заполните форму следующим образом:

| Параметр | Значение |
|----------|----------|
| **Name** | `shoplite-api` (или любое уникальное имя) |
| **Region** | `Frankfurt (EU Central)` или ближайший к вам |
| **Branch** | `main` |
| **Root Directory** | Оставьте пустым (или укажите путь к папке, если проект в подпапке) |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Instance Type** | `Free` (для начала) |

### 3.3 Нажмите "Create Web Service"

Render начнет процесс деплоя. Это займет 2-5 минут.

---

## 4. Настройка переменных окружения

### 4.1 Перейдите в настройки Environment

1. После создания сервиса перейдите на вкладку **"Environment"**
2. Нажмите **"Add Environment Variable"**

### 4.2 Добавьте переменные

Добавьте следующие переменные (по одной):

| Key | Value |
|-----|-------|
| `MONGODB_URI` | `mongodb+srv://username:password@cluster.mongodb.net/shoplite?retryWrites=true&w=majority` |
| `JWT_SECRET` | Сгенерируйте случайную строку (например: `aB3#kL9$pQ2@xY7!zR4%`) |
| `PORT` | `3000` |
| `NODE_ENV` | `production` |

**Важно:** Для `JWT_SECRET` используйте длинную случайную строку!

Генерация JWT_SECRET:
```bash
# В PowerShell
-join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
```

### 4.3 Сохраните изменения

После добавления всех переменных Render автоматически передеплоит приложение.

---

## 5. Настройка MongoDB Atlas

### 5.1 Настройте IP Whitelist

1. Перейдите на [MongoDB Atlas](https://cloud.mongodb.com)
2. Выберите ваш кластер → **Network Access**
3. Нажмите **"Add IP Address"**
4. Выберите **"Allow Access from Anywhere"** (`0.0.0.0/0`)
   - Или добавьте IP адреса Render (смотрите в настройках сервиса)
5. Нажмите **"Confirm"**

### 5.2 Проверьте строку подключения

1. В MongoDB Atlas перейдите в **Database** → **Connect**
2. Выберите **"Connect your application"**
3. Скопируйте строку подключения
4. Замените `<password>` на реальный пароль
5. Добавьте название базы после домена: `/shoplite`

Пример:
```
mongodb+srv://admin:MyPassword123@cluster0.abcde.mongodb.net/shoplite?retryWrites=true&w=majority
```

---

## 6. Проверка деплоя

### 6.1 Получите URL

После успешного деплоя Render предоставит URL вида:
```
https://shoplite-api.onrender.com
```

### 6.2 Проверьте API

Откройте в браузере:
```
https://shoplite-api.onrender.com/api/health
```

Должны увидеть:
```json
{
  "status": "OK",
  "message": "ShopLite API is running",
  "database": "Connected"
}
```

### 6.3 Проверьте фронтенд

Откройте в браузере:
```
https://shoplite-api.onrender.com/
```

Должна открыться главная страница ShopLite.

### 6.4 Проверьте функциональность

1. **Регистрация:** Попробуйте зарегистрировать нового пользователя
2. **Логин:** Войдите в систему
3. **Продукты:** Проверьте загрузку продуктов
4. **Корзина:** Добавьте товар в корзину
5. **Заказ:** Создайте тестовый заказ

---

## 7. Решение проблем

### ❌ Проблема: "Application failed to respond"

**Решение:**
1. Проверьте логи: Dashboard → Logs
2. Убедитесь, что `PORT` не захардкожен в коде
3. Используйте `process.env.PORT || 3000`

### ❌ Проблема: "Cannot connect to MongoDB"

**Решение:**
1. Проверьте `MONGODB_URI` в Environment Variables
2. Убедитесь, что IP адрес разрешен в MongoDB Atlas (0.0.0.0/0)
3. Проверьте, что кластер не на паузе
4. Проверьте правильность пароля (без спецсимволов вроде @, :, /)

### ❌ Проблема: "Module not found"

**Решение:**
1. Убедитесь, что все зависимости в `package.json`
2. Проверьте Build Command: `npm install`
3. Очистите кэш: Settings → Clear build cache & deploy

### ❌ Проблема: "CORS errors"

**Решение:**
В `server.js` должно быть:
```javascript
app.use(cors());
```

### ⚠️ Первый запрос медленный (Free tier)

Render на бесплатном тарифе "засыпает" после 15 минут бездействия.
Первый запрос может занять 30-60 секунд.

**Решение:**
- Используйте UptimeRobot для пинга каждые 5 минут
- Или перейдите на платный план ($7/месяц)

---

## 8. Дополнительные настройки

### 8.1 Настройка Custom Domain (опционально)

1. Купите домен (например, на Namecheap)
2. В Render: Settings → Custom Domain
3. Добавьте ваш домен
4. Настройте DNS записи согласно инструкции Render

### 8.2 Настройка HTTPS

HTTPS включен автоматически! Render предоставляет бесплатный SSL сертификат.

### 8.3 Автоматический деплой

Render автоматически деплоит при каждом push в ветку `main`.

Чтобы отключить:
1. Settings → Build & Deploy
2. Отключите "Auto-Deploy"

---

## 9. Чеклист перед сдачей проекта

- [ ] Проект задеплоен и доступен по URL
- [ ] Все API endpoints работают
- [ ] Фронтенд корректно отображается
- [ ] JWT аутентификация функционирует
- [ ] MongoDB подключена и работает
- [ ] Регистрация/логин работают
- [ ] CRUD операции (Products, Orders, Reviews) работают
- [ ] Роли (client, admin, superadmin) работают корректно
- [ ] README.md обновлен с live URL
- [ ] Postman коллекция создана и экспортирована
- [ ] .env файл НЕ загружен на GitHub
- [ ] .env.example файл создан

---

## 10. Полезные ссылки

- [Render Documentation](https://render.com/docs)
- [MongoDB Atlas Documentation](https://docs.atlas.mongodb.com/)
- [Node.js Deployment Guide](https://render.com/docs/deploy-node-express-app)
- [Environment Variables](https://render.com/docs/environment-variables)

---

## 📝 Обновление README.md

После деплоя обновите README.md:

```markdown
## 🌐 Live Demo

- **Live URL:** https://shoplite-api.onrender.com
- **API Base URL:** https://shoplite-api.onrender.com/api

### Test Accounts

- **Admin:** admin@shoplite.com / password123
- **Client:** user@shoplite.com / password123
```

---

## ✅ Готово!

Теперь ваш проект доступен онлайн по адресу:
```
https://ваш-сервис.onrender.com
```

**Важные напоминания:**
1. Первая загрузка может занять до 1 минуты (free tier)
2. Сервис "засыпает" после 15 минут бездействия
3. Следите за логами в Render Dashboard
4. Для production используйте платный план

Удачи с защитой проекта! 🚀
